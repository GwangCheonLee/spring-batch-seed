spring:
  devtools:
    livereload:
      enabled: true

  config:
    import: optional:file:env/.env.dev[.properties]

  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: always
    chunk-size: ${CHUNK_SIZE:100}
